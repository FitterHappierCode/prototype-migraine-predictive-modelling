# Frontend Setup Guide

## Run the frontend

### Step 1

```bash
cd frontend
```

### Step 2

```bash
npm install
```

### Step 3

```bash
npm run dev
```

The frontend uses `VITE_API_BASE_URL` to connect to the Express backend. If needed, create a `.env` file based on `.env.example`.
